# 이번 주(8/17~8/21) 계획 — Claude 작성

> 이 파일은 Claude가 작성하는 전용 파일이다(`.claude.md`). 같은 폴더의 `260817.md`는 다른 세션/도구가 관리하므로 덮어쓰지 않는다.
> 관련: [10월까지-실행일정.md](../plan/10월까지-실행일정.md) · [alan-실행순서.md](../project/데이터자산화/alan-실행순서.md)

## 오늘(8/17, 월·대체공휴일) — 실시간 ADR 마감

- [ ] [ADR-실시간-매출-데이터-반영-아키텍처.md](../project/데이터자산화/ADR-실시간-매출-데이터-반영-아키텍처.md)를 `제안` 상태로 확정하고 **오늘 종료**한다.
  - 범위: 아키텍처 결정 기록까지. 구현·PoC·실시간 Mart 구축은 별도 승인 전까지 착수하지 않는다.
  - 이 트랙은 오늘부로 데이터 자산화 일정에서 완전히 분리 — 이후 이번 주 계획에는 등장하지 않는다.

## 이번 주 반드시 끝낼 1개 — DA-P0 재착수

실제 업무는 8/18(화)부터 4영업일. `alan-실행순서.md` W1 항목 중 Vonn·Jenna 의존 없이 Alan이 바로 시작할 수 있는 것부터 순서대로.

| 날짜 | 작업 | 구체 행동 | 완료 증거 |
| --- | --- | --- | --- |
| 8/18(화) | `DA-P0-15a` 푸시 cron 이동 | `serverless.yml`의 광고성 배치 cron을 KST 08:00~21:00로 이동. 거래성·CS 예외 경로를 사전 승인 목록으로 뽑아 야간에도 발송되게 분리 | 배포 완료 + D+7 실측(21:00~08:00 KST 발송 0건) 타이머 시작 |
| 8/18(화)~8/19(수) | `DA-P0-06` IDOR 핫픽스 | `user-exercise.service.ts` 세션 조회부에 `accessHistory.user_id !== userId → 400` 체크 추가 | 타인 세션 요청 400 확인, 정상 요청 회귀 통과, 과거 오염 실측 쿼리(`user_exercise_sessions JOIN access_history WHERE s.user_id <> a.user_id`) 결과 기록 |
| 8/19(수) | `DA-P0-02` 결정요청 발송 | 주 시작요일·`started_at` 의미 등 Vonn 결정 3건을 정리해 발송(대기 상태로 던져두고 기다리지 않음) | 요청 발송 기록, 회신 기한 명시 |
| 8/19(수)~8/20(목) | `DA-P0-10` round_number 재넘버링 | 멱등한 재넘버링 스크립트 작성 → 스테이징 실행 → 소요시간·영향 행수 계측 → 재실행 시 변경 0건 확인 | 스테이징 계측 기록, 멱등성 확인 결과 |
| 8/20(목)~8/21(금) | `DA-P0-12` BigQuery 실측 | GCP 권한 3종(`roles/mcp.toolUser`·`bigquery.jobUser`·`bigquery.dataViewer`) 확인 후 적재 실측(events_* 최초·최신일, 30일 볼륨) + user_id 부착률 + DB 조인 샘플 1건 | 실측 메모, 부착률 수치, 조인 분석 결과 |
| 8/21(금) | 주간 마무리 | `DA-P0-10` 스테이징 통과 여부에 따라 프로덕션 반영 여부 결정(되돌릴 수 없는 작업이라 신중 판단). 이번 주 진행 정리, 다음 주(`DA-P0-03` KST 헬퍼) 착수 조건 확인 | 이번 주 완료/미완료 항목 표, 다음 주 착수 조건(P0-01 확정 여부) |

## 이번 주에 하지 않는다

- 실시간 매출 Mart·Continuous Query 구현 (ADR만 오늘 마감)
- `DA-P0-15b`(179개 호출부 인벤토리) — P1 이관 제안, Vonn 승인 대기
- `DA-P0-03`·`04`·`05`(KST 헬퍼·TZ버그) — P0-01 확정(Vonn) 전이라 다음 주로
- CI 잔여 저장소 전환, Habit OS 백엔드 코딩(기획 확정 전)

## 주간 종료 점검

- ADR 마감: `완료 / 미완료`
- DA-P0 5건(15a·06·02·10·12) 중 완료 건수와 미완료 이유:
- P0-10 스테이징→프로덕션 진행 여부와 판단 근거:
- 다음 주(8/24~) 착수 조건이 충족됐는지(P0-01 Vonn 확정, CS 공지 요청):
