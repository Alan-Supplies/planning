.cursor/
node_modules/
.DS_Store
.env
.env.*
!.env.example

# 올리면 안되는 파일
ignore.md

# temp
temp.sql

# Claude Code ask 모드 센티널 (세션 cwd 기준이라 하위 디렉토리도 포함)
**/.claude/.ask

# Claude Code 세션 정리 산출물 (커밋하지 않음)
local/
