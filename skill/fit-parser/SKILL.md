---
name: fit-parser
description: Use when parsing .fit running/cycling activity files, extracting records to CSV, or summarizing sessions/laps as JSON.
---

# FIT Parser

When the user asks to parse a `.fit` file, use `scripts/parse_fit.py`.

Run:

```bash
python3 /path/to/this/skill/scripts/parse_fit.py <fit-file>
```
