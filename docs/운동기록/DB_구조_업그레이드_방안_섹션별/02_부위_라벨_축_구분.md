# 02. 부위 라벨 사전에 축 구분 컬럼 추가

> [전체 목차](README.md) · [통합 원문](../DB_구조_업그레이드_방안.md)
>
> 2026-09-01 보강 — 이 작업이 어느 방향의 몇 단계인지 명시 · 부위 6종 정화안과의 비교 추가 · 함께 보강: [14. 선택·변경된 사항](14_선택_변경된_사항.md) · [15. 남은 결정·조사 항목](15_남은_결정_조사_항목.md)

부위 사전 8행에 축 구분을 넣어 유산소·스트레칭이 부위 집계에 섞이는 경로를 차단하는 작업

## 무엇을 바꾸는가

`body_part` 테이블에 축 구분 컬럼 **`axis`** 추가(값 2종 — 근육 부위 / 운동 성격) · 8행 갱신 · 테이블명은 이번에 변경하지 않음 이 테이블은 이름이 "부위 마스터"지만 실제 내용은 운동 분류 라벨 사전이며, 8종 라벨이 273개 운동을 겹침 없이 분할

|축|라벨|연결 운동|하위 세부부위|
|---|---|---|---|
|근육 부위|가슴·등·어깨·팔·하체·코어 6종|253종|21종|
|운동 성격|유산소·스트레칭 2종|20종(13 + 7)|0종|

## 어떻게 바꾸는가

1. `axis enum('MUSCLE','MODALITY') NOT NULL` 추가

2. 8행 갱신 — 근육 부위 6종은 `MUSCLE` · 유산소·스트레칭 2종은 `MODALITY`

3. 조회 규칙 전환 — 현재 `body_part_id BETWEEN 1 AND 6` 방식을 `axis='MUSCLE'` 조건으로 교체

4. 테이블명 변경은 별도 작업으로 분리 — 외래키 5개( `body_part_detail` · `exercise_body_part` · `machine_body_part` · `user_exercise_session_body_part` · `exercise_recommendation_log_body_part` )와 앱·어드민 코드 참조가 걸려 코드 배포와 동시에 해야 함

## 이 작업의 위치

이 축 컬럼은 `body_part` 를 부위 마스터가 아니라 **운동 분류 라벨 사전으로 인정하는 방향의 1단계**이며 2단계가 테이블명 개칭 · 위 4번이 개칭을 별도 작업으로 분리하고 있으나, 축 컬럼만 들어간 중간 상태에서는 "부위 테이블에 부위가 아닌 행이 있다"는 사실이 그대로 남음

|방향|하는 일|대가|
|---|---|---|
|라벨 사전으로 인정(이번 선택)|테이블명을 `exercise_label` 류로 개칭 · `axis` 유지|외래키 5개 + 앱·어드민 참조 · 코드 배포 동반|
|부위 6종으로 정화|유산소·스트레칭 2행을 성격 축으로 이동|`exercise_body_part` 20종 재매핑 · 앱 필터가 부위 6종 + 성격 2종을 UNION · `axis` 컬럼 자체가 불필요해짐|

유산소·스트레칭이 부위 자리에 들어온 이유 — 8종이 273개 운동을 겹침 없이 분할하므로 이 테이블은 실제로 **앱의 1차 분류 필터**를 떠받치는 유일한 테이블이며, 성격 2종도 행으로 있어야 화면 탭이 만들어짐 · 원인은 "성격을 담을 자리가 없어서"였고 그 자리를 선언하는 것이 이 작업

## 무엇이 좋아지는가

새 부위를 추가할 수 있게 됨 — 지금은 구조적으로 막혀 있음 · 현재 부위 조회 조건이 식별자 범위(1~6)인데, 식별자 7·8이 유산소·스트레칭이므로 전완·목 같은 새 부위는 9번부터 시작함 · 그러면 조건이 연속 범위가 아니게 되어 `IN (1,2,3,4,5,6,9,10)` 같은 열거가 되고, 그 열거가 앱·서버·배치에 흩어져 있 어 부위 하나 추가에 전수 수정과 배포가 필요하며 한 곳을 빠뜨리면 조용히 틀린 값이 나옴 · 축 컬럼이면 어드민에서 행 하나 추가로 앱 필터와 집계에 자동 반영

부위별 운동량 목표 집계가 안전해짐 — 8종 전체를 부위로 집계하면 유산소 13종·스트레칭 7종이 부위 운동량에 섞임 · 축 조건 하나로 자동 제외 앱 부위 배지·필터가 데이터로 결정됨 — 현재 클라이언트에 6개가 하드코딩돼 있어 부위 추가·명칭 변경이 앱 배포를 요구 분류 오염의 재발 차단 — 유산소·스트레칭이 부위 칸에 들어올 수 있었던 원인이 "성격을 담을 자리가 없어서"였음 · 축이 선언되면 신규 등록 시 같은 일이 반복되지 않음

## 주의

- 식별자 순서에 의미를 부여하는 방식은 대리키 특성상 위험 — 데이터 이관·재생성으로 순서가 바뀌면 범위 조건이 조용히 깨짐 위키에는 " **`body_part`** 는 **`exercise_label`** 처럼 활용" 으로 반영 완료(진입점 문서·프로파일·카탈로그 3곳) · 테이블명 부채는 태그 사전 통합 시점에 함께 처리

- **`MODALITY`** 가 뜻이 매칭되지 않게 읽히는 원인은 값 이름이 아니라 테이블명 부채 — 이름이 **`body_part`** 로 남는 한 이 값은 "부위 테이블 안의 부위 아닌 것"을 가리키고 짝인 **`MUSCLE`** 과 층위도 맞지 않음 · 개칭 시 해소되므로 값 이름은 이번에 변경하지 않음(2026-09-01)

- 축 컬럼은 혼재를 표시할 뿐 해소하지 않음 — 한 열거에 두 축이 섞였다는 점에서 [01. 운동 성격 태그 분리](01_운동_성격_태그_분리.md)의 **`functional_tag`** 문제와 같은 구조이나, §01은 테이블 분리로 해소하고 이 작업은 컬럼 하나로 표시만 함 · 의도된 순서 선택이며 해소된 것이 아님

- 정화안이 채택되면 **`axis`** 컬럼은 폐기 대상 — 부위 6행만 남으면 축 구분이 필요 없음 · 그럼에도 지금 착수하는 근거는 명령 1~2건의 저비용([13. 실행 순서와 의존 관계](13_실행_순서와_의존_관계.md) 3순위)이고 새 부위 추가 차단·집계 오염이 현재 실재하는 문제라는 점 · 최종 형태 재평가([15. 남은 결정·조사 항목](15_남은_결정_조사_항목.md))를 기다리지 않고 진행해도 됨

---

[이전](01_운동_성격_태그_분리.md) · [목차](README.md) · [다음](03_유산소_강도.md)
