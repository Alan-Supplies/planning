### KDS는 최신 상태만 필요
과거 상태가 필요한 이유는?
### publish 대상 분류
- 내부 메모 수정
  이건 publish 해야함
이런 업무가 현재 없고 어떤 경우 필요한지 알 수가 없음
- 운영용 보조 컬럼 수정
- 배치성 보정
- 정렬용 값 수정
- 재저장으로 인한 updated_at 갱신
### 재연결시
전체 데이터를 읽는다.
### 순서 보장
순서는 프론트에서 결정한다.
### 누락과 중복
누락을 막는 대신 중복을 선택
프론트에서는 id로 중복 해결
많은 써드 파티 채팅API도 중복처리를 프론트에 일정 부분 떠 넘기는 것 같다. 잘못된 것인가?
### 누락방지를 위한 id + updated_at 커서
이 부분 추가가 outbox 없애는 것보다 손해인가?
