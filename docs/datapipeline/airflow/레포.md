- 메타DB: chart 내장 Postgres(PVC·백업·운영 부담 vs 격리) 대 신규 RDS(비용·prod 승격 경로). dev 만 쓸지 prod 도 같은 형태로 갈지가 갈림길입니다
- worker 이미지: ECR 리포 이름 규칙(CLAUDE.md §ECR — {service}/{app}, -server 금지), 베이스 이미지 버전 고정 정책, 빌드 트리거를 어느 레포에 둘지
- DAG 소스: 별도 레포 + git-sync 대 이미지 포함. dbt 모델과 DAG 를 한 레포에 둘지도 함