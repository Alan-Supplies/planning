1. codebuild 생성
  1. 이름 {domain}-{server/client}-{env}
  예) preppers-order-server-prod
  2. 소스
  github -> repo
  3. vpc 기존 것
    - 서브넷도 기존 것
  4. 추가구성 (환경변수 설정)
   
2. codepipeline
  1. category, template은 그냥 tag 및 기본 형태고 수정가능하니 그냥 느낌으로 선택
  이 아니라 category에 지정 안하는 것이 있다.
  2. 기존 서비스 역할
  code-pipeline-service
  3. 웹훅 이벤트
  건너뛴다 체크X
  4. 빌드 스테이지 추가
  기타 빌드 공급자 -> Aws Codebuild
  아티팩트는 기본 선택되어 있는 SourceArtifact 유지 (x클릭 x)
  5. 테스트 스테이지 - 건너뛰기 (대신buildspec.yml)
  6. 배포 스테이지 - 건너뛰기 (대신buildspec.yml)

3. 알림 설정
  좌측 네비게이션에서
  파이프라인 > 설정 -> 알림