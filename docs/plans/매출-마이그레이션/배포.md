### DB migration
CREATE TABLE `payments` (
  `id` int unsigned NOT NULL AUTO_INCREMENT,
  `store_id` int NOT NULL COMMENT '매장 ID',
  `order_id` int unsigned NOT NULL COMMENT 'customer_order ID',
  `ordered_at` datetime NOT NULL COMMENT '주문 시간',
  `transacted_at` datetime NOT NULL COMMENT '거래 발생 시점 (PAYMENT: 주문시간, REFUND: 환불시간). 매출 집계 기준 시간',
  `parent_id` int unsigned DEFAULT NULL COMMENT '부모 결제 ID (환불/취소의 경우 원본 결제 ID)',
  `transaction_type` varchar(20) NOT NULL COMMENT '거래 유형 (PAYMENT/REFUND/CANCEL_REFUND)',
  `amount` int DEFAULT NULL COMMENT '결제 금액',
  `method` varchar(20) DEFAULT NULL COMMENT '결제 수단 (CARD/KAKAO_PAY/CASH)',
  `card_company` varchar(20) DEFAULT NULL COMMENT '카드사',
  `card_number4` varchar(4) DEFAULT NULL COMMENT '카드번호 뒷 4자리',
  `approved_number` varchar(20) DEFAULT NULL COMMENT '승인번호',
  `installment` varchar(2) DEFAULT NULL COMMENT '할부 개월수',
  `approved_at` datetime DEFAULT NULL COMMENT '승인 시간',
  `status` varchar(20) NOT NULL DEFAULT 'SUCCESS' COMMENT '결제 상태 (SUCCESS/FAILED/PENDING)',
  `platform` varchar(30) NOT NULL COMMENT '플랫폼',
  `service_type` varchar(20) NOT NULL COMMENT '서비스 타입',
  `device` varchar(20) NOT NULL COMMENT '디바이스',
  `description` text COMMENT '설명',
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6) COMMENT '생성일시',
  `updated_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT '수정일시',
  PRIMARY KEY (`id`),
  KEY `idx_status` (`status`),
  KEY `idx_store_approved` (`store_id`,`approved_at`),
  KEY `idx_payments_store_ordered` (`store_id`,`ordered_at`,`status`,`service_type`,`amount`),
  KEY `idx_store_transacted` (`store_id`,`transacted_at`)
) ENGINE=InnoDB AUTO_INCREMENT=695374 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;

ALTER TABLE `customer_order`
ADD `firestore_order_id` varchar(50) NULL COMMENT 'Firestore 주문 ID (마이그레이션 연결용)';

ALTER TABLE `customer_order`
ADD `platform` varchar(30) NOT NULL COMMENT '플랫폼(SELVERS, KIOSK, PREPPERS, YOGIYO, COUPANGEATS, ...)';

ALTER TABLE `customer_order`
ADD `service_type` varchar(20) NOT NULL COMMENT '서비스 타입(DELIVERY, HALL, TOGO)';

ALTER TABLE `customer_order`
ADD `device` varchar(20) NOT NULL COMMENT '디바이스(KIOSK, POS, DELIVERY_APP)';

ALTER TABLE `customer_order`
ADD `ordered_at` datetime NOT NULL COMMENT '주문 시간';

ALTER TABLE `customer_order_food_event`
CHANGE `created_at` `created_at` datetime(6) NOT NULL COMMENT '생성일시'
DEFAULT CURRENT_TIMESTAMP(6);

ALTER TABLE `customer_order_food_event`
CHANGE `updated_at` `updated_at` datetime(6) NULL COMMENT '수정일시'
DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6);

ALTER TABLE `customer_order_food`
CHANGE `created_at` `created_at` datetime(6) NOT NULL COMMENT '생성일시'
DEFAULT CURRENT_TIMESTAMP(6);

ALTER TABLE `customer_order_food`
CHANGE `updated_at` `updated_at` datetime(6) NULL COMMENT '수정일시'
DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6);

ALTER TABLE `customer_order_food_option`
CHANGE `created_at` `created_at` datetime(6) NOT NULL COMMENT '생성일시'
DEFAULT CURRENT_TIMESTAMP(6);

ALTER TABLE `customer_order_food_option`
CHANGE `updated_at` `updated_at` datetime(6) NULL COMMENT '수정일시'
DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6);

CREATE UNIQUE INDEX `UQ_store_firestore_order`
ON `customer_order` (`store_id`, `firestore_order_id`);

### order-server 운영 EKS 올리기
1. preppers-cluster 확인
2. k8/prod
  - service.yaml
  - deployment.yaml
  - apisix-route-api.yaml
  - apisix-route-pri.yaml
  - apisix-route-pub.yaml
3. 
### 람다 적용하기
- preppers-kds-serverless
  orderSheetHandler에 createPreppersOrder적용
  release PR이 예전 main commit 기준이라 조정 필요
### HQ 적용하기
- env파일만 확인


