## TODO
1. IAM 권한 받기
codepipeline:GetPipeline, codepipeline:UpdatePipeline
가능하면 codebuild:BatchGetProjects, codebuild:UpdateProject
확인용으로 CloudFormation/Lambda/EventBridge 조회 권한도 있으면 좋습니다.

2. CI 설정 수정
현재 main → TARGET_ENV=dev를 main → TARGET_ENV=prod로 수정
가능하면 나중에 develop → TARGET_ENV=dev, main → TARGET_ENV=prod로 파이프라인 분리
현재 develop 변경을 원격에 push

3. PR 또는 직접 merge

4. CodeBuild 로그 확인
echo $TARGET_ENV...
Deploying preppers-sales-bot to stage prod
Service deployed to stack preppers-sales-bot-prod

4. prod Lambda/EventBridge 확인
preppers-sales-bot-prod-getStoreSales
preppers-sales-bot-prod-getDeliverySales
두 Lambda에 EventBridge trigger가 붙었는지
EventBridge rule이 Enabled인지
스케줄 실행 확인

5. prod는 실제 실행되어야 함
dev는 실행되더라도 JOB_EXECUTION_MODE=noop 로그만 남아야 함
한 가지 수정하면, “코드빌드 prod 람다로 들어가는지”보다는 CodeBuild가 serverless deploy -s prod를 실행해서 prod CloudFormation/Lambda/EventBridge를 갱신하는지 확인하는 게 정확합니다.