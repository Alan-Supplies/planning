## DONE
- Claude Code ask 모드 인터랙티브 검증 (`docs/ai/claude/ask_mode.md` 6단계)
  - `/ask` 양방향 토글 동작 확인 — 끄기도 막히지 않는다
  - 편집 툴 차단 + 거부 후 대화 맥락 유지 확인
  - 훅 입력 JSON 덤프로 확인: **슬래시 커맨드 `!` 실행은 PreToolUse 훅을 타지 않는다**
- 부수 발견: **Cursor(3.20.17)가 `~/.claude/settings.json` 의 훅을 같이 실행한다**
  - `~/.cursor/hooks.json` 에는 ask-guard 등록이 없는데도 Cursor 세션이 훅을 탄다
  - 원인은 Cursor 의 **Third-Party Imports** 기능 (기본 켜짐). 툴명도 매핑한다 — `Bash`→`Shell`, `Edit`→`Write`
  - 그래서 ask 모드가 Cursor 에 반쪽으로 걸렸다 (편집은 막히고 쉘은 통과)
- ask-guard 에 Cursor 제외 추가 — `cursor_version` 키가 있으면 `exit 0`. 합성 JSON 5케이스로 검증

## TODO
- VSCode source control 다중 폴더 건과 같이 볼 것 — 도구 간 설정 공유 범위
- ask mode 설치
- gymboxx-lib 캐시 적용하기 docs/deployment/build/todo.md
- profile 설정 confirm once 잊지 말자
  "prod": {
      "mysql": {
        "host": "xxxx.supp.kr",
        "user": "xxxx",
        "database": "gymboxx",
      },
      "confirm": "once",
    },

## 회고
오늘도 생각보다 업무 속도가 많이 나지 않음
ai 속도가 조금만 더 빨랐으면 좋겠다.
