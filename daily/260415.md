
## TODO
KDS 신규 아키텍처

- ASIS
```mermaid
graph LR
  subgraph Client["클라이언트"]
    주방(주방)
  end

  subgraph database
    DB[(Firestore)]
  end

  주방 --> |주문구독|DB

```

- TOBE  
KDS_SERVER: KDS 관련 서버  
ORDER_SERVER: 주문 입력 서버

```mermaid
graph LR
  subgraph Client["클라이언트"]
    주방(주방)
  end

  subgraph backend["백엔드"]
    KDS_SERVER
    ORDER_SERVER
    Event구독
  end

  subgraph database
    DB[(MYSQL)]
  end

  주방 --> KDS_SERVER --> DB
  

```

- 주문 구독 방법  
  SSE 통신
- 이벤트 트리거  
  a. ORDER_SERVER에서 주문 입력 즉시
  b. KDS_SERVER에서 DB log 구독

a, b중 어느쪽으로 가야할지 고민

선택 기준

||Firestore|SSE|
|--|--|--|
|장애복구| snapshot만 보면됨 | 직접설계
|구현난이도| 구현되어있음 | snapshot, stream, 복구 직접
|운영비용| firestore console | 연결/전달/재연결 운영
|확장성| 현재상태 모두 소화 | 이벤트 직접 통제로 높음
|보안| 별도의 DB규약 | 서버 중심 일원화


- SSE
```mermaid
graph LR
  subgraph Client
    주방(KDS)
    POS(POS)
  end

  subgraph backend
    KDS_SERVER
    ORDER_SERVER
    POS_SERVER
  end

  subgraph database
    ledger[(MySQL)]
  end

  주방 --> |SSE|KDS_SERVER
  
  KDS_SERVER --> |SSE|ORDER_SERVER

  ORDER_SERVER --> |관리|ledger

  POS --> POS_SERVER --> |생성|ORDER_SERVER
```


- Firestore projection으로 사용
```mermaid
graph LR
  subgraph Client
    주방(KDS)
  end

  subgraph backend
    KDS_SERVER
    ORDER_SERVER
  end

  subgraph database
    ledger[(MySQL)]
    projection[(Firestore)]
  end

  주방 --> KDS_SERVER
  주방 --> |snapshot|projection
  
  KDS_SERVER --> |관리|ledger
  KDS_SERVER --> |수정|projection

  ORDER_SERVER --> |관리|ledger
  ORDER_SERVER --> |생성|projection
```

- A
```mermaid
graph LR
  subgraph Client
    POS(POS)
    KDS(주방 KDS)
  end

  subgraph Backend
    ORDER_SERVER
    KDS_SERVER
    POS_SERVER
  end

  subgraph Database
    ledger[(MySQL)]
  end

  POS --> POS_SERVER --> |주문 생성| ORDER_SERVER
  KDS -->|상태 변경 API| KDS_SERVER

  KDS --> |SSE 구독| KDS_SERVER
  KDS_SERVER -->|SSE 구독| ORDER_SERVER

  KDS_SERVER -->|상태 변경 요청| ORDER_SERVER

  ORDER_SERVER --> |원장 관리| ledger
  ORDER_SERVER --> |snapshot 생성| ledger
  
```
- B
```mermaid
graph LR
  subgraph Client
    POS(POS)
    KDS(주방 KDS)
  end

  subgraph Backend
    ORDER_SERVER
    KDS_SERVER
    POS_SERVER
  end

  subgraph Database
    ledger[(MySQL)]
    projection[(Firestore Projection)]
  end

  POS --> POS_SERVER --> |주문 생성| ORDER_SERVER
  KDS -->|상태 변경 API| KDS_SERVER
  KDS_SERVER -->|상태 변경 요청| ORDER_SERVER

  ORDER_SERVER -->|원장 관리| ledger
  ORDER_SERVER -->|snapshot 갱신| projection
  KDS -->|snapshot 구독| projection
```

### 장애발생
- A
```mermaid
graph LR
  subgraph Client
    KDS(주방 KDS)
  end

  subgraph Backend
    KDS_SERVER
    ORDER_SERVER
  end

  subgraph Database
    ledger[(MySQL)]
  end

  KDS -.연결 끊김 후 재접속.-> KDS_SERVER
  KDS -->|초기 snapshot 조회| KDS_SERVER
  KDS_SERVER -->|현재 주문 조회| ORDER_SERVER
  ORDER_SERVER -->|원장 조회| ledger
  ORDER_SERVER -->|snaphot 생성| ledger
  ORDER_SERVER -->|현재 주문 응답| KDS_SERVER
  KDS_SERVER -->|snapshot 응답| KDS
  KDS_SERVER -->|이후 SSE 재구독| KDS
```

- B
```mermaid
graph LR
  subgraph Client
    KDS(주방 KDS)
  end

  subgraph Database
    projection[(Firestore Projection)]
  end

  KDS -.연결 끊김 후 재접속.-> projection
  KDS -->|현재 주문 snapshot 재조회| projection
  KDS -->|이후 변경분 재구독| projection
```