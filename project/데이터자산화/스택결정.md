1. 원본DB (MYSQL)
2. CDC (AWS DMS)
3. lake 입력은 glue catalog
4. Lake = S3
5. lake sql = athena
6. transformer (airflow + dbt core)
7. warehouse (bigquery)
8. transformer (airflow + dbt core)
9. mart (bigquery)

---

## 1차 PoC 승인 (2026-08-18)

- **대상 데이터**: 기존 gymboxx P0-11(gymboxx-lib 4.29.0 + DDL) 산출물인 `user_exercise_session_history`·`user_exercise_set_history`(신규 개편 운동 기록)만. 다른 테이블(weight_value, challenge_calendar 등)은 이번 PoC 범위 밖.
- **환경**: `gymboxx-dev`. 데이터는 합성/테스트 데이터 — 운영 스냅샷 아님. 이 전제가 바뀌면(운영 데이터로 교체되면) 국외이전 법무 검토(DA-P0-16)를 다시 확인해야 한다.
- **기존 P0 일정과의 관계**: 이 PoC 착수를 위해 **전체 P0 신규작업(P0-06 IDOR, P0-15a 야간 cron 등)을 중단**한다. 단 `P0-10`(round_number 정합화)·`P0-11`(lib+DDL)은 **dev 한정 예외**로 계속 진행 — 이 둘의 프로덕션 배포(스테이징 검증, 6단계 배포, 소비 repo 영향조사 등 `alan-실행순서.md` 원래 DoD)는 별도 승인 전까지 보류.
- **범위 밖(승인 안 됨)**: `ADR-실시간-매출-데이터-반영-아키텍처.md`가 다루는 실시간 매출/Continuous Query 기능은 이 승인과 무관 — 그 ADR은 여전히 `제안` 상태로 별도 승인 대기.
- 근거: 2026-08-18 대화 결정.

