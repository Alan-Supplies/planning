# 2026-08-18~08-21 데이터 자산화 주간 계획

> 소유권: GPT/Codex 전용. GPT는 이 파일만 수정한다.
>
> 장기 컨텍스트: [데이터 자산화 GPT 컨텍스트](260817-10월-업무로드맵.gpt.md)
>
> 전제: 실시간 매출 ADR은 오늘 완료한다. 이번 주 업무시간에는 후속 구현·PoC를 배치하지 않는다.

## 이번 주 반드시 끝낼 결과

**누적되는 법적·보안 위험을 차단하고 P0-10 운영 반영 준비를 끝낸다.**

- `DA-P0-15a`: 야간 푸시 cron 변경 배포 및 D+7 실측 시작
- `DA-P0-06`: 운동 세션 IDOR 수정·테스트·배포
- `DA-P0-10`: round 재번호 스크립트의 스테이징 검증과 운영 실행계획 완료

## 일자별 계획

| 날짜 | WP | 작업 | 완료 증거 |
| --- | --- | --- | --- |
| 8/18(화) | P0-15a | 현재 cron과 거래성 예외 경로 확인 → KST 08:00~21:00 범위로 변경 → 배포 → D+7 측정 시작 | 배포 기록, 변경 전후 cron, 야간 발송 측정 쿼리·대시보드, 판정일 8/25 |
| 8/19(수) | P0-06 | `accessHistory.user_id !== userId` 차단 → 정상·타인 요청 회귀 테스트 → 과거 불일치 쿼리 → 배포 | 타인 요청 400, 정상 요청 성공, 테스트 결과, 불일치 건수, 배포 기록 |
| 8/20(목) | P0-10 | 운영 데이터 현황 조회 → 멱등 재번호 스크립트 작성 → 스테이징 1차 실행 | 대상·영향 행수, 실행시간, `(session_id, round_number)` 중복 0건 |
| 8/21(금) | P0-10 | 동일 스크립트 스테이징 2차 실행 → 멱등성 확인 → 운영 실행 순서·검증 쿼리 작성 | 2차 실행 변경 0건, 운영 대상 예상 건수, 실행·중단 조건, 실행 후 검증 쿼리 |

## 작업별 체크리스트

### DA-P0-15a — 야간 푸시 즉시 차단

- [ ] 현재 `serverless.yml` cron과 KST 환산값 확인
- [ ] 야간에도 필요한 거래성·CS 경로가 같은 cron을 쓰는지 확인
- [ ] 광고성 배치를 KST 08:00~21:00 범위로 이동
- [ ] 설정 문법 또는 배포 패키지 검증
- [ ] 배포 후 다음 실행 시각 확인
- [ ] 21:00~08:00 발송 건수 측정 수단 고정
- [ ] 8/25 D+7 판정 일정 기록

완료 기준: 변경 배포와 실측이 시작되면 구현 완료, D+7 야간 발송 0건이면 최종 완료.

### DA-P0-06 — 운동 세션 IDOR

- [ ] 대상 서비스와 현재 조회 흐름 확인
- [ ] 세션의 `accessHistory.user_id`와 요청 `userId` 비교
- [ ] 타인 세션 요청 400 테스트
- [ ] 정상 소유자 요청 회귀 테스트
- [ ] 과거 소유자 불일치 실측 쿼리 실행
- [ ] 테스트·배포 결과 기록

완료 기준: 타인 요청 차단, 정상 회귀 통과, 과거 영향 범위 기록, 운영 배포.

### DA-P0-10 — round_number 정합화

- [ ] 운영 중복 그룹과 영향 행수 조회
- [ ] 세션별 순서를 결정할 정렬키 고정
- [ ] 2-pass 또는 충돌 없는 재번호 방식 작성
- [ ] 트랜잭션·타임아웃·영향 행수 보호 조건 추가
- [ ] 스테이징 1차 실행 후 중복 0건 확인
- [ ] 스테이징 2차 실행 후 변경 0건 확인
- [ ] 운영 실행 전후 검증 쿼리 작성
- [ ] 운영 실행 시각과 중단 조건 기록

완료 기준: 이번 주에는 스테이징 멱등성 검증과 운영 실행 준비까지. 운영 UPDATE는 다음 주 첫 작업으로 수행한다.

## 이번 주에 하지 않는다

- 실시간 매출 구현·PoC·Continuous Query 구성
- P0-10 운영 UPDATE의 금요일 강행
- P0-12 이후 작업 선행 착수
- P1·Habit OS 별도 구현
- CI 전환

## 여유가 생기면 당길 작업

P0-15a 또는 P0-06이 예상보다 빨리 끝나면 `P0-10`을 즉시 당긴다. P0-10 스테이징 2차 검증까지 끝난 뒤 시간이 남을 때만 `P0-12` BigQuery 실측 쿼리 초안을 작성한다.

## 회사 공유용 문안

> 실시간 매출 반영은 ADR까지 완료하고 구현 범위에서는 제외했습니다. 이번 주는 데이터 자산화 P0에 집중해 야간 푸시 차단과 운동 세션 IDOR를 운영 반영하고, round_number 정합화는 스테이징 멱등성 검증까지 완료할 계획입니다. 운영 데이터 재번호는 금요일 배포를 피하고 다음 주 첫 작업으로 진행합니다.

## 주간 종료 시 갱신

- P0-15a: `완료 / D+7 판정 중 / 미완료`
- P0-06: `완료 / 미완료`
- P0-10: `스테이징 검증 완료 / 진행 중 / 미착수`
- 전체 예상 일정: `유지 / 앞당김 / 지연`
- 다음 주 첫 작업: `P0-10 운영 반영 → P0-12 BigQuery 조인 실측`
