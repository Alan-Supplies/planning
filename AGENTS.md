# Repository working agreements

## Commit and push

- 사용자가 `푸시`, `오늘 것 다 푸시`, `정리하고 푸시`처럼 명시하면 현재 저장소의 작업 결과를 커밋하고 현재 브랜치의 설정된 `origin`에 푸시한 뒤 원격 추적 상태까지 확인한다.
- 같은 푸시 범위에 대해 반복해서 승인을 묻지 않는다. 사용자가 `전부`라고 했으면 현재 작업 트리 전체를 범위로 본다.
- 일일 기록과 무관한 기존 변경은 가능하면 별도 커밋으로 분리해 푸시 범위와 이력을 명확히 한다.
- 커밋 전 비밀키, 액세스 토큰, AWS 키 등 실제 자격 증명 패턴을 검사한다. 실제 자격 증명이 발견된 경우에만 파일과 위험을 알리고 푸시를 멈춘다.
- 샌드박스나 Auto-review가 푸시를 거부하면 같은 승인을 문장만 바꿔 반복 요청하지 않는다. 거부된 정확한 명령과 이유를 알리고, 현재 UI에서 가능하면 `/approve`의 **Auto-review Denials**에서 해당 명령을 1회 승인하도록 안내한다.
- 푸시 요청만 받은 경우 PR을 임의로 만들지 않는다. PR 생성·머지는 사용자가 요청했거나 일일 계획에 명시된 별도 작업으로 처리한다.

## Daily closeout

- 현재 날짜의 `daily/YYMMDD.md`에 실제 완료, 미완료, 다음 행동을 구분해 기록한다.
- `대충 끝남`은 완료로 바꾸지 않고 남은 확인 사항을 후속 행동으로 남긴다.
- 다음 날 계획은 반드시 끝낼 일 1개와 검증 가능한 완료 조건으로 작성한다.
