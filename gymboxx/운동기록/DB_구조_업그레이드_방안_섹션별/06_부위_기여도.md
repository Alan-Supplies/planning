# 06. 부위 기여도 컬럼 추가 · 기존 부하율 원값 보존

> [전체 목차](README.md) · [통합 원문](../DB_구조_업그레이드_방안.md)

합계 1.0 분배율이 만드는 왜곡을 직접·보조 2종 척도로 걷어내는 작업

## 무엇을 바꾸는가

`exercise_body_part_detail` 에 기여도 컬럼 추가(값 2종 — 직접 / 보조) · 기존 `load_ratio` 컬럼은 삭제하지 않고 원값 유지

## 문제의 메커니즘

- FIG 3 등록 부위 수가 부하율을 끌어내리는 구조

<!-- Start of picture text -->
그 운동의 최댓값 평균 최솟값 평균 — 부위를 많이 등록할수록 0.293에서 0.100으로 하락<br>1.000 1.000<br>1.0<br>0.8<br>0.707<br>0.598<br>0.6 0.558 0.540<br>0.4<br>0.293<br>0.2 0.138<br>0.092 0.100<br>0.0<br>부위 1개 부위 2개 부위 3개 부위 4개 부위 5개<br>50종 156종 26종 6종 10종<br>부위 5개 운동의 최댓값 0.540 · 최솟값 0.100 — 절대 0.5 기준이면 주 부위가 사라짐<br><!-- End of picture text -->

- 현재 `load_ratio` 는 운동별 합계가 정확히 1.0인 분배율(248종 전건) · 그래서 같은 운동인데 등록 부위 수에 따라 값이 달라짐

|운동|등록 부위 수|햄스트링|둔근|
|---|---|---|---|
|스미스 데드리프트|2|0.6|0.4|
|바벨 데드리프트|5|0.6|0.1|

- 같은 데드리프트인데 둔근 기여가 4배 다름 · 운동학적 근거가 아니라 부위를 몇 개 등록했는지의 차이 · 스미스는 2개만 등록해 남은 0.4가 둔근에 몰리고, 바벨은 5개로 쪼개져 0.1이 됨

전체 분포도 같은 방향

|등록 부위 수|종목 수|최댓값 평균|최솟값 평균|
|---|---|---|---|
|1|50|1.000|1.000|
|2|156|0.707|0.293|
|3|26|0.598|0.138|
|4|6|0.558|0.092|
|5|10|0.540|0.100|

- 부위를 많이 등록할수록 최솟값이 0.293에서 0.100으로 3배 작아짐 → 여러 근육을 동시에 쓰는 다관절 운동이 불리해지는 역전

## 어떻게 바꾸는가

1. `contribution enum('DIRECT','ASSIST') NOT NULL` 추가

2. 상대 기준으로 일괄 변환 — 그 운동의 최댓값 대비 60% 이상이면 직접, 미만이면 보조(2026-08-21 확정) · 절대 임계값을 쓰지 않는 이유는 아래 대조 표

```
UPDATE exercise_body_part_detail d
  JOIN (SELECT exercise_id, MAX(load_ratio) mx
          FROM exercise_body_part_detail WHERE status='ACTIVE'
         GROUP BY exercise_id) m ON m.exercise_id = d.exercise_id
   SET d.contribution = IF(d.load_ratio >= 0.6 * m.mx, 'DIRECT', 'ASSIST')
 WHERE d.status = 'ACTIVE';
```

1. 판정이 애매한 소수 종목만 수동 확인

2. `load_ratio` 원값은 그대로 유지

기준별 실측 대조 (사용 중 514행 · 248종 · 2026-08-21)

|기준|직접|보조|직접이 하나도 없는 운동|
|---|---|---|---|
|절대 0.5 이상|245행|269행|3종|
|절대 0.4 이상|258행|256행|2종|
|절대 0.3 이상|399행|115행|0종|
|상대—최댓값의 60% 이상|268행|246행|0종|

절대 기준이 성립하지 않는 이유 — 부하율은 운동별 합계가 1.0인 분배율이므로, 부위를 몇 개 등록했는지가 값을 자동으로 끌어내림 · 절대 0.5를 쓰면 부위 를 자세히 등록한 운동일수록 주 부위가 사라짐(부위 5개 등록 운동 10종의 최댓값은 0.30까지 내려감) · 반대로 절대 0.3까지 내리면 직접이 399행으로 늘 어 부위 5개 운동은 사실상 전부 직접이 되어 직접·보조 구분이 무의미해짐

상대 기준의 판정 결과 — 직접 부위가 1개인 운동 230종 · 2개 16종 · 3개 2종 · 직접이 없는 운동 0종 · 다부위 운동의 복수 주 부위를 허용하면서 과도하게 번지지 않음

## 무엇이 좋아지는가

부위별 운동량 목표의 분모와 분자 단위가 일치함 — 목표선 "세부부위당 주 4세트"는 근육군 세트 수 단위인데, 현재 분자는 분배율 가중값이라 단위가 다름 · 직접 1.0 / 보조 0.5 척도로 바꾸면 둘이 같은 단위가 됨

같은 운동의 기여도가 등록 부위 수에 흔들리지 않음 — 위 데드리프트 예에서 둔근 목표 4세트분을 채우는 데 스미스는 10세트, 바벨은 40세트가 필요함 · 같 은 운동인데 4배 차이가 나는 것이 사라짐

다관절 운동이 불리해지는 역전 해소 — 실제로는 여러 근육에 볼륨을 쌓는 운동인데 합계 1.0 제약 때문에 각 부위 값이 작아져 반대로 계산되고 있음 원값 보존으로 되돌릴 수 있음 — 부하율은 15개 값(0.05~1.0)을 가지므로, 2택으로 덮어쓰면 0.1과 0.3의 구분이 영구 소멸 · 원값을 남기면 임계값 사후 조 정과 3단계 척도 전환이 가능

## 주의

- 상대 기준은 값 하나만 보고 판정할 수 없음 — 그 운동의 부위 전체를 봐야 하므로, 판정 결과를 직접 / 보조 값으로 컬럼에 굳혀 저장해야 함 · 조회 시점 계산으로 두면 매 쿼리가 운동 단위 최댓값을 다시 구해야 함 부하율을 수정하면 그 운동의 기여도를 재산출해야 함 — 원값이 남아 있으므로 재산출은 언제든 가능하지만, 운영 절차에 "부하율 변경 시 기여도 재산 출" 단계가 들어가야 함

- 기여도는 운동별 합계 제약 없음 — 한 세트가 여러 근육에 각각 계상되는 것이 정상

## 질문


---

[이전](05_장비_분류.md) · [목차](README.md) · [다음](07_부위_매핑_제약.md)
